# Examples

Placeholder. A minimal, single-screen starter flow (recorded against a trivial local page, so
`npx @reacttv/appreel install` gives a runnable example with zero project-specific setup) lands
here in a follow-up pass — see Phase 5 of the extraction plan.
